/**
 * calWorkspaceCore — pure, DOM-free helpers for the calendar workspace container
 * (config merge across calendars, Apex payload adapter).
 *
 * Import as: `import { mergeColorRules, toGenericEvents } from 'c/calWorkspaceCore';`
 */
export * from './mergeConfig';
export * from './eventAdapter';